[package]
name = "find-stutter"
version = "0.2.0"
edition = "2021"
description = "System stutter monitor CLI - Slint UI overlay"
# 默认 cargo run / cargo install 跑这个（GUI 入口；export / stats 是子命令）
default-run = "find-stutter"

[[bin]]
name = "find-stutter"
path = "src/main.rs"

[lib]
name = "find_stutter"
path = "src/lib.rs"

[dependencies]
find-stutter-core = { path = "../core" }
find-stutter-ui = { path = "../ui" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
log = "0.4"
env_logger = "0.11"
