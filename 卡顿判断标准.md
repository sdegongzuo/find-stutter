# 卡顿判断标准（find-stutter）

> 本文档依据 `crates/core/src/detector.rs`、`crates/core/src/types.rs`、`config.toml` 的**实际代码**整理，定义 find-stutter 如何判断一次「卡顿」、各触发信号的取舍与阈值，以及误报治理的演进。
> 本文取代并统合此前的零散说明：`卡顿根因分析-PRD.md` 聚焦「为什么卡顿」（根因归因），`docs/memory-stutter-detection.md` 仅覆盖内存维度；本文件覆盖**「什么算一次卡顿」**的完整判定标准。
>
> 最后更新：v0.3.x（含本次「吞吐 spike 不再单独成卡顿」升级）。

---

## 1. 核心定义

**卡顿（stutter）** = 系统某类资源出现**持续**饱和或异常，足以导致用户感知卡顿或前台窗口无响应。

- 单次卡顿是一次「会话」：从某个压力信号首次触发（`stutter_start`）开始，到全部信号回落且满足持续时长后结束，落库为一条 `StutterEvent`。
- **不是单帧尖峰**：必须持续 ≥ `sustained_seconds`（默认 3s）才落库，避免瞬时抖动误报。
- 与「根因归因」解耦：本文件只回答「是否算卡顿 + 触发了哪些信号」；「是哪个进程/软件导致的」由 F-RC1~F-RC16 根因分析负责。

---

## 2. 触发生命周期（`Detector::analyze`）

每 tick 采集一个 `Sample`，送入 `analyze`：

1. **硬阈值检测** `check_hard_thresholds`：瞬时硬阈值 + 滞回（CPU、内存、提交电荷、分页、磁盘繁忙、DPC、中断、上下文切换、温度降频）。见 §4 表。
2. **突增检测** `check_spike`：滑动基线比率突增（CPU spike、网络 spike、内存可用骤降 spike），需「连续确认（recent 10 中 ≥6 超阈值）+ 滞回」。
3. **触发/维持跟踪**：任一 cause 非空 → 进入或维持卡顿跟踪，并累积 culprit 进程（按 CPU / 内存 top 去重合并，最多 6 个）。
4. **结束并落库**：当所有 cause 回落为空且持续 ≥ `sustained_seconds` → 生成事件（定级 severity、计算 `primary_cause` / `cause_kinds` / `onset_ts` / `culprits` 落库）。
5. **压力闸门（v0.3.x 新增）**：生成事件前，若整个卡顿窗口内**没有任何「系统压力类」cause**（见 §5 分类），则**丢弃本次跟踪**——纯吞吐 spike（网络 / 磁盘写）本身不构成卡顿。

> 卡顿不足 `sustained_seconds` 即结束：同样清空状态、不落库（与闸门共用 `return None` 路径）。

---

## 3. 触发信号总表

所有阈值集中在 `config.toml`，不在代码中写死，便于按机型 / 场景调参。

| # | 信号 | 字段 / 来源 | 阈值（config.toml） | 口径 | 滞回 | 是否压力类 cause | 说明 |
|---|------|-------------|---------------------|------|------|------------------|------|
| 1 | CPU 使用率 | `cpu_usage` | `cpu_threshold = 90.0`（`>`） | 瞬时百分比 | 进入 >90；退出 <80（`cpu_hysteresis=10`） | ✅ `CpuHigh` | 主信号之一 |
| 2 | CPU 突增 | `cpu_usage` 滑动基线 | `spike_ratio = 3.0` | 比率（需 ≥6/10 确认） | 回落到阈值×0.5 以下才解除 | ✅ `CpuSpike` | 与硬阈值互补，捕捉短时飙高 |
| 3 | 内存使用率 | `mem_usage_percent` | `mem_threshold_percent = 90.0`（`>`） | 瞬时百分比 | 无 | ✅ `MemLow` | 覆盖「大内存机可用 MB 仍高、使用率已爆表」漏报 |
| 4 | 物理可用内存 | `mem_available_mb` | `mem_threshold_mb = 500`（`<`） | 绝对下限 | 无 | ✅ `MemLow` | 兜底小内存机 |
| 5 | 提交电荷 | `commit_bytes / commit_limit` | `commit_threshold_percent = 90.0`（`>`） | 瞬时比值 | 无 | ✅ `MemLow` | 比「可用物理内存归零」更早预警 |
| 6 | 分页活动速率 | `page_reads_per_sec` | `page_reads_threshold = 300.0`（`>`） | 速率（流量） | 无 | ✅ `MemLow` | **需内存/磁盘压力证据共存**才记 cause（放大器，见 §6） |
| 7 | 磁盘真繁忙度 | `disk_busy_percent` / `disk_avg_io_ms` | `disk_busy_threshold_percent = 95.0` **或** `disk_io_threshold_ms = 50.0` | 复合（OR 进入 / AND 退出） | 进入 >阈值；退出 <阈值×0.5 | ✅ `DiskBusy` | 吞吐高 ≠ 饱和，**繁忙度/IO 延迟才是真信号** |
| 8 | DPC 时间 | `dpc_percent` | `dpc_threshold_percent = 10.0`（`>`） | 瞬时百分比 | ×0.5 滞回 | ✅ `DpcInterrupt` | DPC 挤占普通线程（CPU 不忙但系统卡） |
| 9 | 中断时间 | `interrupt_percent` | `interrupt_threshold_percent = 10.0`（`>`） | 瞬时百分比 | ×0.5 滞回 | ✅ `InterruptStorm` | 中断挤占普通线程 |
| 10 | 上下文切换风暴 | `context_switches_per_sec` | `context_switch_threshold_per_sec = 50000.0`（`>`） | 瞬时 | ×0.5 滞回 | ✅ `ContextSwitchStorm` | 切换风暴拖垮调度 |
| 11 | 温度降频 | `cpu_temp` + `cpu_freq_mhz` | `thermal_threshold_celsius = 85.0` | 三条件同时 | 无中间带 | ✅ `ThermalThrottle` | 温度 >85 **且** 频率 < 峰值×`thermal_freq_drop_ratio`（默认 0.85）**且** 负载 >50% |
| 12 | 前台冻结 | Win32 探测（`ui_freeze_timeout_ms`） | 探测无响应即触发 | 限频 2s | 无 | ✅ `UiFrozen` | **仅作伴随诊断**，绝不单独成 cause（必须伴随其它 cause） |
| 13 | 网络突增 | `net_sent_bps + net_recv_bps` | `spike_ratio = 3.0` + `spike_min_bps = 2000000` | 比率（≥6/10 确认） | ×0.5 滞回 | ❌ `NetSpike` | **异步 I/O 突发（下载/构建/git）非卡顿**，v0.3.x 起不再单独成卡顿 |
| 14 | 磁盘写突增 | `disk_write_bps` | `disk_rate_spike_ratio`（保留项） | — | — | ❌ `DiskSpike` | **代码已移除实时触发**（吞吐≠饱和），仅 `config.toml` 保留供 what-if 参考 |
| 15 | 软件/驱动/硬件根因 | 进程指纹 + 事件日志（F-RC14） | `handle_leak_threshold` / `handle_leak_growth_threshold` / `gdi_leak_threshold` 等 | 指纹趋势 / 白名单 | — | ✅ `ProcessHandleLeak` / `GdiObjectLeak` / `DriverTimeout` / `ServiceCrash` / `DiskIoError` / `HardwareError` | 用户可行动的真根因，参与主因 |

---

## 4. 滞回（hysteresis）模型

滞回的目的是：**避免信号在阈值附近反复横跳时，反复开始/结束卡顿记录（反复误报）。**

- **CPU 硬阈值滞回**：进入 `> cpu_threshold`；退出 `< cpu_threshold - cpu_hysteresis`（带内维持激活）。带内文案标注「滞回保持」，避免 "85% > 90%" 的矛盾文案。
- **系统信号滞回**（磁盘繁忙复合 / DPC / 中断 / 上下文切换）：进入 `> threshold`；退出 `< threshold × sys_signal_hysteresis_ratio`（默认 0.5）。
- **spike 滞回**：触发后需 recent 均值回落到 `threshold × 0.5` 以下才解除；且触发本身需 recent 10 样本中 ≥6 个超阈值（连续确认），防止零星尖峰与瞬时抖动。
- **温度降频**：无中间带，三条件同成立才激活、否则解除（避免单一高温误判）。

---

## 5. 压力类 / 非压力类 cause 分类（v0.3.x 闸门依据）

`CauseKind::is_pressure()` 判定某类 cause 是否「能直接造成系统无响应」：

- **压力类（is_pressure = true）**：`CpuHigh` / `CpuSpike` / `MemLow`（含可用、使用率、提交、分页）/ `DiskBusy` / `DpcInterrupt` / `InterruptStorm` / `ContextSwitchStorm` / `ThermalThrottle` / `UiFrozen`，以及全部软件/驱动/硬件级根因（F-RC14，属用户可行动的真根因）。
- **非压力类（is_pressure = false）**：`NetSpike`、`DiskSpike`、`GpuHigh`。它们本质是异步 I/O 突发或 GPU 占用，不直接阻塞系统调度，**单凭它们不应记录一次卡顿事件**。

> 闸门只过滤「落库」，不删除信号：当非压力类 spike 与压力类 cause 共存于同一卡顿窗口时，spike 仍作为**附加 cause** 保留（事件里能看到「网络突增」字样），仅「纯吞吐 spike」被丢弃。

---

## 6. 严重程度（severity）判定

`determine_severity(causes, duration_ms)`：

| 条件 | 级别 |
|------|------|
| cause 数 ≥ 3 **或** 持续 > 30s | `Critical` |
| cause 数 ≥ 2 **或** 持续 > 10s | `Major` |
| 否则 | `Minor` |

- cause 计数包含所有被记录的 cause（压力类 + 附加的非压力类如网络 spike）。
- **通知门槛** `min_severity`（默认 `major`）仅控制是否弹窗提醒，**不影响事件落库**。误报治理必须改检测逻辑（如 §7），不能靠调高通知门槛「眼不见为净」。

---

## 7. 误报治理演进（重要）

判断标准不是一次定死的，而是随实测反馈持续收紧。本轮升级是第 D 阶段。

- **阶段 A（swap 存量）**：`swap_usage_percent` 曾作硬触发 → 存量 ≠ 压力、易误报且虚高 severity → **降级为仅展示**，改用分页速率（流量）。
- **阶段 B（分页速率）**：`Page Reads/sec` 瞬时抖动极大，开发机/模拟器正常负载也频繁超阈值，但磁盘空闲/提交不高时系统不卡 → 改为**需内存/磁盘压力证据共存**才记 cause（退化为「放大器」）。
- **阶段 C（磁盘写 spike）**：吞吐高 ≠ 磁盘饱和 → **移除磁盘写 spike 实时触发**，改用磁盘真繁忙度（`% Disk Time` / IO 延迟）。
- **阶段 D（v0.3.x，本次升级）**：网络 spike（及残留的磁盘写 spike）属于纯吞吐突发——下载、构建、git、备份等正常高吞吐被记成「卡顿」。新增**压力闸门**：纯吞吐 spike 窗口内若没有任何系统压力类 cause，则丢弃本次跟踪，不再落库为卡顿事件。

**实证数据**：今日（2026-08-15）`today_events.csv` 记录的卡顿，绝大多数为 `Network spike` / `Disk write spike`，对应行的 CPU 仅 20–75%、内存仅 40–67%（均健康，用户无感）。这正是阶段 D 要消除的误报。

---

## 8. 部署注意事项（关键）

检测器逻辑位于 service 侧。**代码落地 ≠ 运行态生效**：

- 必须**停服并重装新服务二进制**（P5 约束）后，新判断标准才会真正参与判定。
- 当前 `today_events.csv` 仍出现 `Disk write spike`，说明线上跑的是**旧服务二进制**（当前代码已移除该 cause）。即便只升级网络 spike 闸门，也需重装服务才能消除残留误报。

---

## 9. 阈值可调性（config.toml 关键项）

```toml
[sampling]
interval_ms = 1000            # 采样间隔（秒级）；空闲时放大 slow_interval_factor 倍

[detection]
cpu_threshold = 90.0          # CPU 使用率硬阈值（>）
cpu_hysteresis = 10.0         # CPU 滞回（百分点）
mem_threshold_percent = 90.0  # 内存使用率（>）
mem_threshold_mb = 500        # 可用内存绝对下限（<）
commit_threshold_percent = 90.0
page_reads_threshold = 300.0  # 分页速率（>），需内存/磁盘压力证据共存
spike_ratio = 3.0             # 突增倍数（CPU/网络/内存可用）
spike_min_bps = 2000000       # 网络 spike 绝对下限（B/s），防零头误报
sustained_seconds = 3         # 持续多久才落库为一次卡顿
disk_busy_threshold_percent = 95.0
disk_io_threshold_ms = 50.0
dpc_threshold_percent = 10.0
interrupt_threshold_percent = 10.0
context_switch_threshold_per_sec = 50000.0
thermal_threshold_celsius = 85.0
handle_leak_threshold = 10000
handle_leak_growth_threshold = 2000   # 句柄趋势判定：后半段净增超此值才算真泄漏
gdi_leak_threshold = 10000

[notifications]
min_severity = "major"        # 仅控制弹窗，不影响落库
```

> 注：`docs/memory-stutter-detection.md` 中 `page_reads_threshold` 写的是 `50.0`，已过时——当前代码与 `config.toml` 默认值均为 `300.0`（阶段 B 上调）。本文以代码为准。

---

## 10. 数据落库

- `stutter_events`：卡顿事件表，含 `causes` / `cause_kinds` / `primary_cause` / `onset_ts` / `severity` / `culprits` / `duration_ms` 等，保留 **7 天**（PRD §3.4.6）。
- `samples`：每 tick 全量字段，保留 **30 天**，供分析页与 what-if（F-RC12）按可调阈值重算（what-if 仅用单帧、不含 spike 类）。
- 软件根因子表（`process_modules` / `windows_events` / `stack_samples` / `root_cause_reports`）：随 `stutter_events` 级联清理（FK `ON DELETE CASCADE`）。
