# 卡顿判断标准（find-stutter）

> 本文档定义 find-stutter 如何判断一次「卡顿」：触发信号、阈值、滞回、压力闸门与落库口径，依据 `crates/core/src/detector.rs`、`crates/core/src/types.rs`、`config.toml` 的**实际代码**整理。
> 「是哪个进程/软件导致的」由《卡顿根因分析-PRD.md》（F-RC1~F-RC16）负责，不在本文范围。

---

## 1. 核心定义

**卡顿（stutter）** = 系统某类资源出现**持续**饱和或异常，足以导致用户感知卡顿或前台窗口无响应。

- 单次卡顿是一次「会话」：从某个压力信号首次触发（`stutter_start`）开始，到全部信号回落且满足持续时长后结束，落库为一条 `StutterEvent`。
- **不是单帧尖峰**：必须持续 ≥ `sustained_seconds`（默认 3s）才落库，避免瞬时抖动误报。
- **水位 ≠ 事件**：长期高位（如内存常年 90%+）是稳态而非卡顿，受滞回与稳态抑制约束（见 §4）。
- 与「根因归因」解耦：本文件只回答「是否算卡顿 + 触发了哪些信号」。

---

## 2. 触发生命周期（`Detector::analyze`）

每 tick 采集一个 `Sample`，送入 `analyze`：

0. **采样中断防护**：与上一 tick 的墙钟间隔 > `max_tick_gap_secs`（默认 15s）判定为系统睡眠/挂起——清空全部跟踪状态重新评估、**绝不落库**（睡眠时长不得计入卡顿 duration）。
1. **硬阈值检测** `check_hard_thresholds`：瞬时硬阈值 + 滞回（CPU、内存、分页、磁盘繁忙、DPC、中断、上下文切换、温度降频）。见 §3 表。
2. **突增检测** `check_spike`：滑动基线比率突增（CPU spike、网络 spike），需「连续确认（recent 10 中 ≥6 超阈值）+ 单样本绝对下限 + 滞回」。
3. **触发/维持跟踪**：任一 cause 非空 → 进入或维持卡顿跟踪，并累积 culprit 进程（按 CPU / 内存 top 去重合并，最多 6 个）。
4. **结束并落库**：当所有 cause 回落为空且持续 ≥ `sustained_seconds` → 生成事件（定级 severity、计算 `primary_cause` / `cause_kinds` / `onset_ts` / `culprits` 落库）。
5. **压力闸门**：生成事件前，若整个卡顿窗口内**没有任何「系统压力类」cause**（见 §5 分类），则**丢弃本次跟踪**——纯吞吐 spike（网络 / 磁盘写）本身不构成卡顿。

> 卡顿不足 `sustained_seconds` 即结束：同样清空状态、不落库（与闸门共用 `return None` 路径）。

---

## 3. 触发信号总表

所有阈值集中在 `config.toml`，不在代码中写死，便于按机型 / 场景调参。

| # | 信号 | 字段 / 来源 | 阈值（config.toml） | 口径 | 滞回 | 是否压力类 cause | 说明 |
|---|------|-------------|---------------------|------|------|------------------|------|
| 1 | CPU 使用率 | `cpu_usage` | `cpu_threshold = 90.0`（`>`） | 瞬时百分比 | 进入 >90；退出 <80（`cpu_hysteresis=10`），带内文案「滞回保持」 | ✅ `CpuHigh` | 主信号之一 |
| 2 | CPU 突增 | `cpu_usage` 滑动基线 | `spike_ratio = 3.0` + `spike_min_cpu_percent = 50.0` | 比率 + 绝对下限（≥6/10 确认且单样本 ≥50%） | 回落到阈值×0.5 以下才解除 | ✅ `CpuSpike` | 与硬阈值互补；低使用率的纯比率突增（如 5%→16%）不触发 |
| 3 | 内存使用率 | `mem_usage_percent` | `mem_threshold_percent = 90.0`（`>`） | 水位（进入线） | 进入 >90；退出 ≤85；连续活跃 > `mem_chronic_seconds`（60s）稳态抑制锁存 | ✅ `MemLow` | 覆盖「大内存机可用 MB 仍高、使用率已爆表」漏报 |
| 4 | 物理可用内存 | `mem_available_mb` | `mem_threshold_mb = 500`（`<`） | 水位（进入线） | 进入 <500；退出 ≥600；同受稳态抑制 | ✅ `MemLow` | 兜底小内存机 |
| 5 | 提交电荷 | `commit_bytes / commit_limit` | `commit_threshold_percent = 90.0` | 仅作分页压力证据 | — | —（不产 cause） | commit 高只是记账上限逼近，本身不影响性能；阈值保留作分页证据判定线，真压力由可用内存/分页信号触发 |
| 6 | 分页活动速率 | `page_reads_per_sec` | `page_reads_threshold = 300.0`（`>`） | 速率（流量） | 无 | ✅ `MemLow` | **需内存/磁盘压力证据共存**才记 cause（放大器语义） |
| 7 | 磁盘真繁忙度 | `disk_busy_percent` / `disk_avg_io_ms` | `disk_busy_threshold_percent = 95.0` **或** `disk_io_threshold_ms = 50.0` | 复合（OR 进入 / AND 退出） | 进入 >阈值；退出 <阈值×0.5 | ✅ `DiskBusy` | 吞吐高 ≠ 饱和，繁忙度/IO 延迟才是真信号 |
| 8 | DPC 时间 | `dpc_percent` | `dpc_threshold_percent = 10.0`（`>`） | 瞬时百分比 | ×0.5 滞回，带内文案「滞回保持」 | ✅ `DpcInterrupt` | DPC 挤占普通线程（CPU 不忙但系统卡） |
| 9 | 中断时间 | `interrupt_percent` | `interrupt_threshold_percent = 10.0`（`>`） | 瞬时百分比 | ×0.5 滞回，带内文案「滞回保持」 | ✅ `InterruptStorm` | 中断挤占普通线程 |
| 10 | 上下文切换风暴 | `context_switches_per_sec ÷ 逻辑核数` | `context_switch_threshold_per_core = 10000.0`（/s/核，`>`）；**首次激活还需 CPU 侧压力证据**（总使用率 > `context_switch_min_cpu_percent = 80`，或 DPC/中断越线） | 按核归一瞬时 | ×0.5 滞回（按核口径），带内文案「滞回保持」 | ✅ `ContextSwitchStorm` | 切换风暴拖垮调度。绝对阈值（原 50000/s）在多核机骑日常基线致连环误报，2026-08-17 改机器相对口径：核数取样本 `cpu_per_core` 长度（采集自带、随机器自校准，缺失退回 `available_parallelism`，实时与 what-if 共用同一函数），激活后的滞回退出不受证据影响 |
| 11 | 温度降频 | `cpu_temp` + `cpu_freq_mhz` | `thermal_threshold_celsius = 85.0` | 三条件同时 | 无中间带 | ✅ `ThermalThrottle` | 温度 >85 **且** 频率 < 峰值×`thermal_freq_drop_ratio`（默认 0.85）**且** 负载 >50%；频率峰值带慢衰减 |
| 12 | 前台冻结 | Win32 探测（`ui_freeze_timeout_ms`） | 探测无响应即触发 | 限频 2s | 无 | ✅ `UiFrozen` | **仅作伴随诊断**，绝不单独成 cause（必须伴随其它 cause） |
| 13 | 网络突增 | `net_sent_bps + net_recv_bps` | `spike_ratio = 3.0` + `spike_min_bps = 2000000` | 比率 + 绝对下限（≥6/10 确认） | ×0.5 滞回 | ❌ `NetSpike` | 异步 I/O 突发（下载/构建/git）非卡顿，不单独成事件 |
| 14 | 磁盘写突增 | `disk_write_bps` | `disk_rate_spike_ratio`（保留项） | — | — | ❌ `DiskSpike` | 吞吐 ≠ 饱和，不作为触发信号；配置项仅供 what-if 参考 |
| 15 | 软件/驱动/硬件根因 | 进程指纹 + 事件日志（F-RC14） | `handle_leak_threshold` / `handle_leak_growth_threshold` / `gdi_leak_threshold` 等 | 指纹趋势 / 白名单 | — | ✅ `ProcessHandleLeak` / `GdiObjectLeak` / `DriverTimeout` / `ServiceCrash` / `DiskIoError` / `HardwareError` | 用户可行动的真根因，参与主因 |

---

## 4. 滞回与稳态抑制模型

滞回的目的是：**避免信号在阈值附近反复横跳时，反复开始/结束卡顿记录（反复误报）**。

- **CPU 硬阈值滞回**：进入 `> cpu_threshold`；退出 `< cpu_threshold - cpu_hysteresis`（带内维持激活）。带内文案标注「滞回保持」，避免 "85% > 90%" 的矛盾文案。
- **系统信号滞回**（磁盘繁忙复合 / DPC / 中断 / 上下文切换）：进入 `> threshold`；退出 `< threshold × sys_signal_hysteresis_ratio`（默认 0.5）。滞回带内维持激活的 cause 文案标注「滞回保持」（数值已回落但未到退出线，避免 "8% > 10%" 式矛盾文案）。上下文切换的**进入**还需 CPU 侧压力证据（见 §3 第 10 行），激活后的滞回退出不受证据影响。
- **内存水位滞回 + 稳态抑制**：使用率进入 `>90%` / 退出 `≤85%`，可用内存进入 `<500MB` / 退出 `≥600MB`（两条件任一越过进入线即激活，全部回落到退出线以下才解除）。连续活跃超过 `mem_chronic_seconds`（默认 60s）进入**稳态抑制锁存**：停止发射内存 cause，直到回落到退出线以下才解锁——**水位是稳态、颠簸才是事件**，常年 90%+ 内存的机器每次越线只产生一条有界事件。分页（paging）不受抑制影响，真颠簸由 paging cause 独立触发。
- **滞回带最长保持**：任何信号当前值**已低于进入线**、仅靠滞回维持激活超过 `hysteresis_hold_max_secs`（默认 15s）即强制解除；重新越过进入线重置计时。滞回是防抖工具，**不是延长卡顿的工具**；持续真饱和（如 CPU 一直 >90%）不受影响。
- **spike 滞回**：触发后需 recent 均值回落到 `threshold × 0.5` 以下才解除；触发本身需 recent 10 样本中 ≥6 个超阈值（连续确认）+ 单样本绝对下限（网络 `spike_min_bps` / CPU `spike_min_cpu_percent`），防止零星尖峰与低绝对值纯比率误报。
- **温度降频**：无中间带，三条件同成立才激活、否则解除（避免单一高温误判）；频率峰值带慢衰减（`thermal_freq_peak_decay`），陈旧睿频峰值不会长期误判「掉档」。

---

## 5. 压力类 / 非压力类 cause 分类（闸门依据）

`CauseKind::is_pressure()` 判定某类 cause 是否「能直接造成系统无响应」：

- **压力类（is_pressure = true）**：`CpuHigh` / `CpuSpike` / `MemLow`（含可用、使用率、分页；提交电荷不产 cause）/ `DiskBusy` / `DpcInterrupt` / `InterruptStorm` / `ContextSwitchStorm` / `ThermalThrottle` / `UiFrozen`，以及全部软件/驱动/硬件级根因（F-RC14，属用户可行动的真根因）。
- **非压力类（is_pressure = false）**：`NetSpike`、`DiskSpike`、`GpuHigh`。它们本质是异步 I/O 突发或 GPU 占用，不直接阻塞系统调度，**单凭它们不应记录一次卡顿事件**。

> 闸门只过滤「落库」，不删除信号：当非压力类 spike 与压力类 cause 共存于同一卡顿窗口时，spike 仍作为**附加 cause** 保留（事件里能看到「网络突增」字样），仅「纯吞吐 spike」被丢弃。

---

## 6. 严重程度（severity）判定

`determine_severity(causes, duration_ms)`：

| 条件 | 级别 |
|------|------|
| 压力类 cause 数 ≥ 3 **或** 持续 > 30s | `Critical` |
| 压力类 cause 数 ≥ 2 **或** 持续 > 10s | `Major` |
| 否则 | `Minor` |

- cause 计数**只计压力类**：非压力类（网络 spike 等纯吞吐信号）作为附加 cause 保留在事件里，但不参与严重度计数——避免「CpuHigh + 网络 spike」凑 2 因虚增为 Major。无法映射到 `CauseKind` 的文本按压力类计（防御）。
- **通知门槛** `min_severity`（默认 `major`）仅控制是否弹窗提醒，**不影响事件落库**。调整判定必须改检测逻辑/阈值，不能靠调高通知门槛「眼不见为净」。

---

## 7. 部署注意事项（关键）

检测器逻辑位于 service 侧。**代码落地 ≠ 运行态生效**：修改判定逻辑后，必须**停服并重装新服务二进制**（P5 约束），新判断标准才会真正参与判定。

推荐一条命令完成（停服释放 exe 锁 → `rtk cargo build --release` → 重装启动，自动 UAC 提权；见 `UPGRADE.md`）：

```bash
find-stutter upgrade          # 或 --no-build 跳过构建，仅用已有 release exe 重装
```

---

## 8. 阈值可调性（config.toml 关键项）

```toml
[sampling]
interval_ms = 1000            # 采样间隔（秒级）；空闲时放大 slow_interval_factor 倍

[detection]
cpu_threshold = 90.0          # CPU 使用率硬阈值（>）
cpu_hysteresis = 10.0         # CPU 滞回（百分点）
mem_threshold_percent = 90.0  # 内存使用率进入线（>）；退出线 = 90 - mem_hysteresis_percent
mem_threshold_mb = 500        # 可用内存进入线（<）；退出线 = 500 × mem_available_exit_ratio
mem_hysteresis_percent = 5.0      # 内存使用率滞回（90 进 / 85 出）
mem_available_exit_ratio = 1.2    # 可用内存退出线比例（500 进 / 600 出）
mem_chronic_seconds = 60      # 内存水位稳态抑制：连续活跃超时锁存
commit_threshold_percent = 90.0  # 提交电荷——仅作分页压力证据判定线
page_reads_threshold = 300.0  # 分页速率（>），需内存/磁盘压力证据共存
spike_ratio = 3.0             # 突增倍数（CPU/网络）
spike_min_bps = 2000000       # 网络 spike 绝对下限（B/s），防零头误报
spike_min_cpu_percent = 50.0  # CPU spike 单样本绝对下限（%）
sustained_seconds = 3         # 持续多久才落库为一次卡顿
hysteresis_hold_max_secs = 15 # 滞回带最长保持（通用）
max_tick_gap_secs = 15        # 采样中断防护：间隔超此值判定睡眠/挂起，不落库
win_event_backtrack_secs = 10 # 事件日志回溯窗口
disk_busy_threshold_percent = 95.0
disk_io_threshold_ms = 50.0
dpc_threshold_percent = 10.0
interrupt_threshold_percent = 10.0
context_switch_threshold_per_core = 10000.0 # 切换风暴进入线（/s/核，按逻辑核归一）
context_switch_min_cpu_percent = 80.0  # 切换风暴首次激活的 CPU 侧压力证据线（%）
# 注：旧键 context_switch_threshold_per_sec 已随 2026-08-17 按核归一移除，
# 配置文件中的残留键会被 serde 静默忽略，请改用上面两个新键
thermal_threshold_celsius = 85.0
thermal_freq_drop_ratio = 0.85
thermal_freq_peak_decay = 0.995  # 频率峰值慢衰减；1.0 = 不衰减
handle_leak_threshold = 10000
handle_leak_growth_threshold = 2000   # 句柄趋势判定：后半段净增超此值才算真泄漏
gdi_leak_threshold = 10000

[notifications]
min_severity = "major"        # 仅控制弹窗，不影响落库
```

---

## 9. 数据落库

- `stutter_events`：卡顿事件表，含 `causes` / `cause_kinds` / `primary_cause` / `onset_ts` / `severity` / `culprits` / `duration_ms` 等，保留 **7 天**（PRD §3.4.6）。
- `samples`：每 tick 全量字段，保留 **30 天**，供分析页与 what-if（F-RC12）按可调阈值重算（what-if 为单帧重算，不含 spike 类与跨帧滞回/稳态抑制）。
- 软件根因子表（`process_modules` / `windows_events` / `stack_samples` / `root_cause_reports`）：随 `stutter_events` 级联清理（FK `ON DELETE CASCADE`）。
